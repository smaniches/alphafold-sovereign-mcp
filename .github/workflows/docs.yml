name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "src/**"
      - "ARCHITECTURE.md"
      - "CHANGELOG.md"
      - "CITATION.cff"
      - "CODE_OF_CONDUCT.md"
      - "CONTRIBUTING.md"
      - "GOVERNANCE.md"
      - "LIMITATIONS.md"
      - "PRIVACY.md"
      - "SECURITY.md"
      - "STATUS.md"
      - "SUPPORT.md"
      - "AUDIT.md"
      - "README.md"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write   # for mkdocs gh-deploy to push to gh-pages

jobs:
  build-and-deploy:
    name: Build & deploy mkdocs to gh-pages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0   # mkdocs needs the full history for revision dates

      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install package + docs deps
        run: uv sync --extra docs

      - name: Build (non-strict; warnings are OK while content stabilises)
        run: uv run mkdocs build --clean

      - name: Deploy to gh-pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          uv run mkdocs gh-deploy --force --no-history
