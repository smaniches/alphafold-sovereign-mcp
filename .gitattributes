# Recorded upstream HTTP fixtures for the golden examples: real public-API
# responses, provenance-stamped with a per-response SHA-256, regenerated by
# scripts/record_golden_examples.py and proven byte-for-byte by tests/golden/.
# They are machine data, not hand-reviewed source, so mark them generated:
# GitHub then collapses them in pull-request diffs. The reviewable surface is
# the harness code, the example READMEs, and the small expected.json outputs.
examples/golden/*/cassette.json linguist-generated=true
